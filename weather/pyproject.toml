[project]
name = "weather"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx",
    "langchain",
    "langchain-mcp-adapters",
    "langchain-openai",
    "mcp[cli]",
    "openai",
    "python-dotenv",
]
